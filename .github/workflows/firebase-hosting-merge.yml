# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main

env:
  REACT_APP_WS_URL: wss://mda-pong-exercice.onrender.com/ws
  REACT_APP_API_URL: https://mda-pong-exercice.onrender.com/

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cd client/ && yarn && yarn build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_MD_PONG_EXERCISE }}'
          channelId: live
          projectId: md-pong-exercise
